spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/ticketwave_order_es}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
